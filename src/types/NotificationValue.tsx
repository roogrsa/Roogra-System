export interface NotificationValues {
  title: string;
  message: string;
  date: string;
  customers: boolean;
  advertisers: boolean;
  delegates: boolean;
  observers: boolean;
  supervisors: boolean;
}
